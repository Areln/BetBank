@{
    ViewData["Title"] = "Home Page";
}


@model BetBank.Models.ViewModels.HomePageModel

@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


<h1 style="text-align: center;">Bet Bank</h1>

<div class="tcontainer">
    <div class="ticker-wrap">
        <div class="ticker-move">
            @foreach (TickerGames item in Model.TickerGames)
            {
                <div class="ticker-item">
                    @item.TimeOfEvent<br />
                    @item.HomeTeam @item.HomeOdds<br />
                    <div style="text-align:center">at</div>
                    @item.AwayTeam @item.AwayOdds<br />

                </div>
            }
        </div>
    </div>
</div>

@if (SignInManager.IsSignedIn(User))
{

    <div class="topnav" id="myTopnav">
        <a href="../Home/Index">Home</a>
        <a href="../Event/NFLIndex">NFL</a>
        <a href="../Event/NBAIndex">NBA</a>
        <a href="../Event/ESportsIndex">E-Sports</a>
    </div>

    <div>
        <img class="graphImage" src="../img/sportsgraph.png" alt="sports graph" width="600" height="475" />
    </div>

    @*@if (SignInManager.IsSignedIn(User))
        {*@
    <div class="userStats">
        <h3>User Stats</h3>
        <table class="table">
            <tr>
                <th>Bet History</th>
                <th>Total W/L</th>
                <th>Total ROI</th>
            </tr>
            <tr>
                <td>Today Results</td>
                <td>@Model.UserStats.TodayWLRecord</td>
                <td>@Model.UserStats.ReturnOnInvestment</td>
            </tr>
            <tr>
                <td>Last 7 Days</td>
                <td>@Model.UserStats.YesterdayWLRecord</td>
                <td>@Model.UserStats.ReturnOnInvestment</td>
            </tr>
            <tr>
                <td>All Time</td>
                <td>@Model.UserStats.AllTimeWLRecord</td>
                <td>@Model.UserStats.ReturnOnInvestment</td>
            </tr>
        </table>

        <p>Leagues and game type user<br /> bets W/L records</p>

        <a href="../Bet/BetHistory">View Bet History</a>
    </div>

    <div class="graphLinks">
        <ul>
            <li><a href="#">1 Day ||</a></li>
            <li><a href="#">1 Week ||</a></li>
            <li><a href="#">1 Month ||</a></li>
            <li><a href="#">1 Year</a></li>
        </ul>
    </div>

    <div class="betLinkDiv">
        <ul>
            <li><a href="#">Track Bets</a></li>
            <li><a href="#">Added to BB</a></li>
            <li><a href="#">Withdrew from BB</a></li>
        </ul>
    </div>
    //}

}

else
{
    <div class="sitePreview">
        <img src="../img/sportsBetting.jfif" alt="Alternate Text" width="1125"/>
    </div>
}